---
layout: base
---

- for post in site.posts
  .post
    .preface
      .date
        = post.date.strftime( '%d. %B %Y' )
      %h1
        %a{:href=>post.url}
          = post.title
      .tags
        Tags:
        - for tag in post.tags
          - unless tag.nil?
            %a{:href=>tag.primary_page.url}
              = tag
    .content
      ~post.content
